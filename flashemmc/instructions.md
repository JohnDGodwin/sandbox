Flash image to an sd and boot

login: ubuntu/ubuntu

run `./flash_emmc.sh`

wait for the image to flash (approx 5.3GB)

shutdown the system and remove the sd card

Power on the system and it should boot from the onboard emmc
